{
	"name": "Azure game",
	"version": "1.0.0",
	"description": "",
	"license": "ISC",
	"author": "firepolo",
	"private": true,
	"workspaces": [
		"server",
		"client",
		"shared"
	],
	"scripts": {
		"dev:server": "npm run dev -w=@game/server",
		"dev:client": "npm run dev -w=@game/client",
		"build:server": "npm run build -w=@game/server",
		"build:client": "npm run build -w=@game/client",
		"build": "npm run build:server && npm run build:client"
	}
}
